# CFO Flight Deck — TODO

All P1 and P2 items are complete.

## Done

- **TODO 2** — Modularized `app.js` into 18 ES modules under `src/`, added Vite build (9.6 KB gzipped)
- **TODO 3** — 131 Vitest unit tests across 7 test files, all passing
- **TODO 4** — All 7 error hardening gaps fixed (BOM strip, try/catch, file validation, NaN/Infinity guards, etc.)
- **TODO 5** — Date format auto-detection + user override in column mapping panel
- **TODO 6** — Full localStorage persistence (mapping, aliases, grain, filters)
- **TODO 7** — 25 currencies including PKR, zero-decimal currency support
- **TODO 8** — 150ms debounce on inputs, memoized summaries, delegated event handling
- **TODO 9** — URL state sync for all filter state
- **TODO 10** — Vite + GitHub Pages CI/CD via GitHub Actions
- **UI overhaul** — Obsidian dark theme (CSS variables, glass panels, ghost borders, 20px radii, Newsreader + Work Sans fonts)
- **Bug fixes** — `signedPercent` crash on non-finite values, concentration insight skip, head checklist event listener leak
